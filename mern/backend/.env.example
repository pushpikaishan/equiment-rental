# Backend environment configuration

# Server
PORT=5000
MONGO_URI=mongodb://localhost:27017/equipment_rental

# Mailer (choose one)
# Option 1: Single SMTP connection string
# SMTP_URL=smtp://USER:PASS@smtp.example.com:587

# Option 2: Host/Port/Auth fields
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_smtp_user
# SMTP_PASS=your_smtp_password

# From address for outgoing emails
MAIL_FROM="Equipment Rental <no-reply@example.com>"

# Optional SMS webhook (for simple SMS gateway integration)
# The service should accept POST { to: string, message: string }
# SMS_WEBHOOK_URL=https://sms-gateway.example.com/api/send
